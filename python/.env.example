APP_ENV=local

# DB 接続（docker-compose の mysql サービスに合わせた初期値）
DB_HOST=mysql
DB_PORT=3306
DB_NAME=ses_matching
DB_USER=root
DB_PASSWORD=secret

# AWS 設定
AWS_REGION=ap-northeast-1

# SSM パラメータ名（Google Maps API キーの格納先）
GOOGLE_MAPS_API_KEY_SSM_NAME=Nexus-google-maps-key
