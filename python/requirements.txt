# Web フレームワーク
fastapi==0.115.0

# ASGI サーバー（Nginx + PHP-FPM 相当）
uvicorn==0.30.0

# データバリデーション
pydantic==2.7.0

# HTTP クライアント（Laravel からの呼び出し受信用）
httpx==0.27.0

# .env ファイルの読み込み
python-dotenv==1.0.0

# AWS SDK（Bedrock / SSM 呼び出し用）
boto3

# ORM・DB 接続（Python のデファクトスタンダード）
sqlalchemy==2.0.36

# MySQL ドライバ（SQLAlchemy と組み合わせて使用）
pymysql==1.1.1
