<?php

/*
|--------------------------------------------------------------------------
| 認証系メッセージ（日本語）
|--------------------------------------------------------------------------
|
| Breeze 標準の認証フローが参照する文言を日本語化する。
| - failed   : メール／パスワード不一致（LoginRequest::authenticate が throw）
| - throttle : ログイン試行回数の上限到達（スロットリング）
|
| ※ auth.password（現在パスワードの照合失敗）はパスワード確認・変更フロー専用で、
|   当該機能（ConfirmPassword / Profile）は削除済みのため本アプリでは呼ばれない。
|   未使用キーは持たせず failed / throttle のみを定義する。
|
*/

return [
    'failed' => 'メールアドレスまたはパスワードが正しくありません',
    // ⚠️ Laravel 既定は :seconds（秒）だが、これは窓が60秒である前提の文言。
    //    本アプリはアカウント単位の第2段（15分窓）を重ねており、最大で「900 秒後」と
    //    表示されて読み手に暗算を要求するため、:minutes（切り上げた分）で表示する。
    //    LoginRequest::ensureIsNotRateLimited() が両方の値を渡している。
    'throttle' => 'ログイン試行回数が上限に達しました。約 :minutes 分後に再度お試しください。',
];
