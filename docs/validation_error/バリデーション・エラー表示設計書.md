# バリデーション・エラー表示設計書

> 最終更新：2026-05-27

## 概要
- 本ドキュメントは全機能共通のバリデーションおよびエラー表示仕様を定義する
- バリデーションは Laravel（FormRequest）で実装する
- エラーは Inertia.js を通じて Props としてフロントへ渡す

---

## 共通仕様

### 必須/任意の制御方針（重要）
- WF上の「必須」バッジは初期設定値を表すものであり、常に必須であることを意味しない。
- 必須/任意の制御は以下の2層で行う。

**第1層：システム固定必須（DBレベルでNOT NULL）**
- 管理者も変更不可。バリデーションルールに `required` をハードコードする。

| 対象フォーム | フィールド |
|-------------|-----------|
| 人材登録・編集 | `name`（氏名）/ `name_kana` / `status` / `main_user_id` |
| 案件登録・編集 | `name`（案件名）/ `status` / `main_user_id` |

**第2層：動的必須（form_field_settings テーブルで制御）**
- 第1層以外のすべてのフィールドが対象。
- 管理者がマスタ管理画面から必須/任意をトグルで変更可能（QA #65確定）。
- バリデーションルールは FormRequest 内で form_field_settings を参照して動的に組み立てる。
- WF上で「必須」バッジが表示されているフィールドであっても、第2層に該当する場合は `required` をハードコードしてはならない。
- **`※動的` フィールドは任意設定時に `nullable` を付与する**。ENUM型も例外なく `nullable / in:...` の組み合わせで定義すること。

> **根拠：** DB設計書 §1-2 / QA #65・#82確定

### FormRequest 実装方針（参考）

```php
// app/Http/Requests/EngineerRequest.php
public function rules(): array
{
    // 第1層：システム固定必須（ハードコード）
    $rules = [
        'name'         => ['required', 'string', 'max:100'],
        'name_kana'    => ['required', 'string', 'regex:/^[ァ-ヶー　]+$/u'],
        'status'       => ['required', 'in:proposable,interviewing,not_proposable'],
        'main_user_id' => ['required', 'exists:users,id'],
    ];

    // 第2層：form_field_settings を参照して動的に組み立て
    // field_key は form_field_settings テーブルの field_key カラムと1対1で対応する
    $dynamicFields = [
        'birth_date'          => ['date'],
        'nearest_station'     => ['string', 'max:100'],
        'nearest_line'        => ['string', 'max:100'],
        'available_from'      => ['date'],
        'has_negotiation_exp' => ['boolean'],
        'appeal_note'         => ['string', 'max:4000'],
        'desired_rate'        => ['integer', 'min:0'],
        'work_styles'         => ['array'],
        'remarks'             => ['string', 'max:1000']
    ];

    foreach ($dynamicFields as $field => $fieldRules) {
        $isRequired = FormFieldSetting::isRequired('engineer', $field);
        $rules[$field] = array_merge(
            [$isRequired ? 'required' : 'nullable'],
            $fieldRules
        );
    }

    // 工程経験：proc_experience の1設定で proc_* 6フィールドをまとめて制御する
    // WF_12のフォーム設定タブでも「経験工程」として1つのトグルで管理する
    $procRequired = FormFieldSetting::isRequired('engineer', 'proc_experience');
    $procFields = [
        'proc_requirements', 'proc_basic_design', 'proc_detail_design',
        'proc_development',  'proc_testing',       'proc_maintenance',
    ];
    foreach ($procFields as $field) {
        $rules[$field] = [$procRequired ? 'required' : 'nullable', 'boolean'];
    }

    return $rules;
}
```

---

### エラー構造（Inertia）
```
errors.name
errors.skills.0.label
errors.skills.0.detail
errors.proc_requirements
errors.main_user_id
```

### Props構造
| フィールド | 型 | 説明 |
|-----------|----|----|
| errors | object | バリデーションエラー |
| errors.{field} | string | 先頭のエラーメッセージ |

### エラー表示ルール（UI）
| 項目 | 内容 |
|------|------|
| 表示位置 | 入力項目の直下 |
| 表示件数 | 各フィールドごとに先頭1件を表示 |
| 表示タイミング | submit後 |
| スクロール | 最初のエラーへスクロール |
| 表示形式 | 赤文字表示 |

### エラーメッセージ方針
- 日本語で表示する
- 項目名＋エラー内容の形式とする
- 丁寧語で統一する（〜してください）
- フィールド単位でエラーを表示する
- エラーメッセージはLaravelのlangファイルで管理する

---

## 1. ログイン（Login）

---

## 2. ダッシュボード

---

## 3. 人材管理（Engineer）

### 対象画面
| 画面 | URL | メソッド |
|------|-----|--------|
| 人材登録 | /engineers/create | POST |
| 人材編集 | /engineers/{id}/edit | PUT |

### バリデーション定義

> ※ 「※動的」のフィールドは form_field_settings テーブルを参照して必須/任意を制御する（第2層）。  
> ※ 「※動的」フィールドは任意設定時に `nullable` を付与する。ENUM型も `nullable / in:...` の組み合わせとする。  
> ※ `fieldSettings` の各キーは form_field_settings テーブルの `field_key` カラムと1対1で対応する。  
> システム固定必須（DBレベルで NOT NULL）は `name` / `name_kana` / `status` / `main_user_id` のみ。それ以外のフィールドはDBレベルではNULL許容とする。

#### 基本情報

| フィールド | バリデーション | エラーメッセージ例 |
|-----------|-------------|----------------|
| name | required / string / max:100 | 氏名は必須です |
| name_kana | required / string / regex:/^[ァ-ヶー　]+$/u | カナで入力してください |
| birth_date | ※動的 / date | 正しい日付を入力してください |
| nearest_station | ※動的 / string / max:100 | 最寄駅は100文字以内で入力してください |
| nearest_line | ※動的 / string / max:100 | 路線名は100文字以内で入力してください |
| available_from | ※動的 / date | 稼働可能日を入力してください |

> `name_kana` の入力正規化：バリデーション前に半角スペースを全角スペースへ変換する
> （FormRequest の `prepareForValidation()` で実装）。
> ユーザーは半角・全角どちらでも入力可能。DBには全角スペースで統一して保存する。

#### スキル情報

> **skills の件数バリデーションについて：**
> skills の必須/任意は form_field_settings で制御する（DB設計書 §1-2）。
> - form_field_settings で skills が必須の場合：空配列・省略ともにエラー
> - form_field_settings で skills が任意の場合：空配列・省略ともに許容（全削除として扱う）
> - PUT時に skills: [] を送信した場合、既存スキルを全件削除する
> - PUT時に skills キーを省略した場合も同様に全件削除として扱う

| フィールド | バリデーション | エラーメッセージ例 |
|-----------|-------------|----------------|
| skills | nullable / array | - |
| skills.*.label | required_with:skills.*.detail / string / max:15 | スキル名は15文字以内で入力してください。detail入力時はlabelも必須 |
| skills.*.detail | nullable / string / max:500 | スキル詳細は500文字以内で入力してください |
| proc_requirements | ※動的 / boolean | 不正な値です |
| proc_basic_design | ※動的 / boolean | 不正な値です |
| proc_detail_design | ※動的 / boolean | 不正な値です |
| proc_development | ※動的 / boolean | 不正な値です |
| proc_testing | ※動的 / boolean | 不正な値です |
| proc_maintenance | ※動的 / boolean | 不正な値です |
| has_negotiation_exp | ※動的 / boolean | 選択してください |

> **工程経験（proc_*）の動的制御について：**
> proc_* 6フィールドの必須/任意は `form_field_settings` の `field_key = "proc_experience"` の1設定でまとめて制御する。
> WF_12のフォーム設定タブでも「経験工程」として1つのトグルで管理する。
> `proc_experience` の `is_required = true` の場合、6フィールドすべてに `required` を付与する。
> フィールドごとの個別設定は行わない。

#### PR

| フィールド | バリデーション | エラーメッセージ例 |
|-----------|-------------|----------------|
| appeal_note | ※動的 / string / max:4000 | 4000文字以内で入力してください |

#### 希望条件

| フィールド | バリデーション | エラーメッセージ例 |
|-----------|-------------|----------------|
| desired_rate | ※動的 / integer / min:0 | 正しい金額を入力してください |
| work_styles | ※動的 / array | 不正な値です |
| work_styles.* | in:onsite,hybrid,remote | 不正な値です |
| remarks | ※動的 / string / max:1000 | 1000文字以内で入力してください |

#### 管理情報

| フィールド | バリデーション | エラーメッセージ例 |
|-----------|-------------|----------------|
| status | required / in:proposable,interviewing,not_proposable | ステータスを選択してください |
| main_user_id | required / exists:users,id | 主担当を選択してください |
| sub_user_id | nullable / exists:users,id / different:main_user_id | 主担当と異なるユーザーを選択してください |

### 更新時のバリデーション方針

| 項目 | 内容 |
|------|------|
| 基本ルール | PUTは全フィールド送信を前提とする。未入力項目も含めて送信し、nullable項目は null を許容する |
| skills | PUTで送信された配列全体を検証する |
| 必須制御 | システム固定必須（name / name_kana / status / main_user_id）を除き、必須/任意は form_field_settings の設定値を元に FormRequest で動的制御する |
| 動的フィールドのnullable | ※動的フィールドは任意設定時に nullable を付与する。ENUM型も nullable / in:... の組み合わせとする |
| sub_user_id | main_user_id と異なること |

---

## 4. 案件管理（Project）

### 対象画面

| 画面 | URL | メソッド |
|------|-----|--------|
| 案件登録 | /projects/create | POST |
| 案件編集 | /projects/{id}/edit | PUT |

---

### バリデーション定義

#### 基本情報

| フィールド | バリデーション | エラーメッセージ例 |
|-----------|-------------|----------------|
| name | required / string / max:255 | 案件名は必須です |
| client_name | ※動的 / string / max:100 | 顧客名は100文字以内で入力してください |
| headcount | ※動的 / integer / min:0 | 正しい人数を入力してください |
| start_date | ※動的 / date | 正しい日付を入力してください |

#### 契約条件

> **単価バリデーションについて：**
> `rate_is_negotiable` が `true` の場合：`rate_min` / `rate_max` は送信不要（nullとして扱う）。`rate_note` に任意テキストを入力可能。  
> `rate_is_negotiable` が `false` の場合：`rate_min` / `rate_max` が必須/任意の制御対象となる（form_field_settings の `rate` 設定で制御）。  
> `rate_min` と `rate_max` を両方入力する場合は `rate_min` ≤ `rate_max` であること。  

| フィールド | バリデーション | エラーメッセージ例 |
|-----------|-------------|----------------|
| rate_is_negotiable | nullable / boolean | 不正な値です |
| rate_min | ※動的 / integer / min:0 / lte:rate_max | 正しい金額を入力してください |
| rate_max | ※動的 / integer / min:0 / gte:rate_min | 正しい金額を入力してください |
| rate_note | nullable / string / max:100 | 100文字以内で入力してください |
| commercial_flow | ※動的 / nullable / in:prime,secondary,tertiary,other | 商流を選択してください |

#### 勤務条件

> **勤務地バリデーションについて：**
> `work_style` が `remote`（フルリモート）の場合：`work_location_line` / `work_location_station` はバリデーション対象外（フロントでクリア済みのためnullとして扱う）。  
> `work_style` が `onsite` または `hybrid` の場合：`work_location_station`（最寄駅）を必須として扱う（WF_07注記確定）。  
> ※ `work_location_station` の条件付き必須は `form_field_settings` の動的制御ではなく、業務ルールによる固定の条件付き必須とする。  

| フィールド | バリデーション | エラーメッセージ例 |
|-----------|-------------|----------------|
| work_style | ※動的 / nullable / in:onsite,hybrid,remote | 稼働形態を選択してください |
| work_location_line | nullable / string / max:100 | 路線名は100文字以内で入力してください |
| work_location_station | required_if:work_style,onsite / required_if:work_style,hybrid / nullable / string / max:100 | 常駐・一部リモートの場合は最寄駅を入力してください |
| interview_count | ※動的 / integer / min:0 | 正しい回数を入力してください |

#### スキル要件

> **スキルの件数バリデーションについて：**
> `required_skills` / `preferred_skills` の必須/任意は `form_field_settings` で制御する（DB設計書 §1-2）。  
> - `form_field_settings` で必須の場合：空配列・省略ともにエラー  
> - `form_field_settings` で任意の場合：空配列・省略ともに許容（全削除として扱う）  
> - PUT時に空配列 [] または省略した場合、既存スキルを全件削除する  

| フィールド | バリデーション | エラーメッセージ例 |
|-----------|-------------|----------------|
| required_skills | nullable / array | - |
| required_skills.*.label | required_with:required_skills.*.detail / string / max:15 | スキル名は15文字以内で入力してください。detail入力時はlabelも必須 |
| required_skills.*.detail | nullable / string / max:500 | スキル詳細は500文字以内で入力してください |
| preferred_skills | nullable / array | - |
| preferred_skills.*.label | required_with:preferred_skills.*.detail / string / max:15 | スキル名は15文字以内で入力してください。detail入力時はlabelも必須 |
| preferred_skills.*.detail | nullable / string / max:500 | スキル詳細は500文字以内で入力してください |
| proc_requirements | ※動的 / boolean | 不正な値です |
| proc_basic_design | ※動的 / boolean | 不正な値です |
| proc_detail_design | ※動的 / boolean | 不正な値です |
| proc_development | ※動的 / boolean | 不正な値です |
| proc_testing | ※動的 / boolean | 不正な値です |
| proc_maintenance | ※動的 / boolean | 不正な値です |
| negotiation_required | ※動的 / boolean | 選択してください |
| description | ※動的 / string | 業務内容詳細を入力してください |
| work_env | ※動的 / string | 稼働環境を入力してください |

> **対象工程（proc_*）の動的制御について：**
> proc_* 6フィールドの必須/任意は `form_field_settings` の `field_key = "proc_experience"` の1設定でまとめて制御する。
> WF_12のフォーム設定タブでも「対象工程」として1つのトグルで管理する。
> `proc_experience` の `is_required = true` の場合、6フィールドすべてに `required` を付与する。
> フィールドごとの個別設定は行わない。
> 実装方針は人材管理の FormRequest（`EngineerRequest`）と同様。共通仕様セクションの参考コードを参照すること。

#### 管理情報

| フィールド | バリデーション | エラーメッセージ例 |
|-----------|-------------|----------------|
| status | required / in:open,closed,pending | ステータスを選択してください |
| main_user_id | required / exists:users,id | 主担当を選択してください |
| sub_user_id | nullable / exists:users,id / different:main_user_id | 主担当と異なるユーザーを選択してください |

#### 就業条件

| フィールド | バリデーション | エラーメッセージ例 |
|-----------|-------------|----------------|
| billing_range | ※動的 / string / max:100 | 精算幅は100文字以内で入力してください |
| remarks | ※動的 / string | 特記事項を入力してください |

---

### 更新時のバリデーション方針

| 項目 | 内容 |
|-----|------|
| 基本ルール | PUTは全フィールド送信を前提とする。未入力項目も含めて送信し、nullable項目は null を許容する |
| required_skills / preferred_skills | PUTで送信された配列全体を検証する。空配列・省略は全削除として扱う |
| 必須制御 | システム固定必須（name / status / main_user_id）を除き、必須/任意は form_field_settings の設定値を元に FormRequest で動的制御する |
| 動的フィールドのnullable | ※動的フィールドは任意設定時に nullable を付与する。ENUM型（commercial_flow・work_style等）も nullable / in:... の組み合わせとする |
| sub_user_id | main_user_id と異なること |
| 単価整合性 | rate_is_negotiable が false の場合、rate_min ≤ rate_max であること |
| 勤務地 | work_style が onsite / hybrid の場合、work_location_station を必須とする（業務ルール固定・form_field_settings の動的制御対象外） |

---

## 5. マッチング（Matching）

---

## 6. 進捗管理（Pipeline）

---

## 7. 保存検索条件（SavedSearch）

---

## 8. CSV入出力

### POST /csv/engineers/import・POST /csv/projects/import（共通）

#### ファイルバリデーション

| フィールド | バリデーション | エラーメッセージ例 |
|-----------|-------------|----------------|
| file | required / file / mimes:csv,txt / max:5120 | CSVファイルを選択してください |
| file | （拡張子チェック） | .csvファイルを選択してください |
| file | （サイズチェック）max:5120（KB） | ファイルサイズは5MB以内にしてください |

> **`mimes:csv,txt` について**：LaravelのMIMEタイプ判定では `text/csv` が `txt` と判定されることがあるため `csv,txt` の両方を許容する。拡張子が `.csv` かどうかは追加チェックを実装すること。  
> **文字コードチェック**：UTF-8（BOMあり・なし）以外の文字コード（Shift-JIS等）は処理前に検出してエラーを返すこと。エラーメッセージ例：「UTF-8形式のCSVファイルをアップロードしてください」

---

#### 行バリデーション（人材インポート）

CSVの各行に対してバリデーションを適用する。1行でもエラーがある場合は全行ロールバックし、エラー内容を返す。

> **CSVインポートと `form_field_settings` の関係**：フォーム画面の必須/任意制御（form_field_settings）はCSVインポートに適用しない。CSVは `name` / `name_kana` / `status` / `main_user_id` のみ必須とし、それ以外はNULL許容とする（DB設計書 §1-2準拠）。  
> **空セルの扱い**：空セルは `null` として処理し、既存データをNULLで上書きする（空文字とnullは同一扱い）。  
> **`name_kana` の正規化**：フォーム登録と同様に、インポート時も半角スペースを全角スペースへ変換してからバリデーションを適用すること。

**新規追加行（id列が空）のバリデーション**

| CSVカラム | 対応DBカラム | バリデーション | エラーメッセージ例 |
|---------|-----------|-------------|----------------|
| id | id | 空であること（新規追加として扱う） | - |
| 氏名 | name | required / string / max:100 | 氏名は必須です |
| 氏名カナ | name_kana | required / string / max:100 / regex:/^[ァ-ヶー　]+$/u | カナで入力してください |
| 生年月日 | birth_date | nullable / date | 正しい日付を入力してください（YYYY-MM-DD） |
| 最寄駅 | nearest_station | nullable / string / max:100 | - |
| 路線 | nearest_line | nullable / string / max:100 | - |
| 稼働可能時期 | available_from | nullable / date | 正しい日付を入力してください（YYYY-MM-DD） |
| 希望単価 | desired_rate | nullable / integer / min:0 | 正しい金額を入力してください |
| アピールポイント | appeal_note | nullable / string | - |
| AI要約 | ai_summary | インポート時は無視する（上書き不可） | AI生成値のため手動更新不可 |
| 特記事項 | remarks | nullable / string | - |
| ステータス | status | required / in:proposable,interviewing,not_proposable | ステータスは必須です |
| 主担当ID | main_user_id | required / integer / exists:users,id | 主担当に存在するユーザーIDを指定してください |
| サブ担当ID | sub_user_id | nullable / integer / exists:users,id / different:main_user_id | 主担当と異なるユーザーIDを指定してください |
| 常駐可 | work_style_onsite | nullable / in:0,1 | 0または1を入力してください |
| 一部リモート可 | work_style_hybrid | nullable / in:0,1 | 0または1を入力してください |
| フルリモート希望 | work_style_remote | nullable / in:0,1 | 0または1を入力してください |
| 要件定義経験 | proc_requirements | nullable / in:0,1 | 0または1を入力してください |
| 基本設計経験 | proc_basic_design | nullable / in:0,1 | 0または1を入力してください |
| 詳細設計経験 | proc_detail_design | nullable / in:0,1 | 0または1を入力してください |
| 開発経験 | proc_development | nullable / in:0,1 | 0または1を入力してください |
| テスト経験 | proc_testing | nullable / in:0,1 | 0または1を入力してください |
| 保守運用経験 | proc_maintenance | nullable / in:0,1 | 0または1を入力してください |
| 顧客折衝経験 | has_negotiation_exp | nullable / in:0,1 | 0または1を入力してください |

> **スキルについて**：現時点ではCSV対象外とする。スキルの登録・編集は人材詳細画面から行うこと。スキルキーワードはエクスポートの絞り込み条件としては使用可能（TBD#1参照）。

**更新行（id列に既存ID）のバリデーション**

| 項目 | 内容 |
|---|---|
| id | integer / exists:engineers,id。存在しないIDは全行ロールバックしてエラーを返す |
| 他フィールド | 新規追加行と同じバリデーションルールを適用 |
| スキル | CSV対象外のため更新しない（TBD#1参照） |

---

#### 行バリデーション（案件インポート）

> **CSVインポートと `form_field_settings` の関係**：フォーム画面の必須/任意制御（form_field_settings）はCSVインポートに適用しない。CSVは `name` / `status` / `main_user_id` のみ必須とし、それ以外はNULL許容とする（DB設計書 §1-2準拠）。  
> **空セルの扱い**：空セルは `null` として処理し、既存データをNULLで上書きする（空文字とnullは同一扱い）。  

**新規追加行（id列が空）のバリデーション**

| CSVカラム | 対応DBカラム | バリデーション | エラーメッセージ例 |
|---------|-----------|-------------|----------------|
| id | id | 空であること | - |
| 案件名 | name | required / string / max:255 | 案件名は必須です |
| 顧客名 | client_name | nullable / string / max:100 | - |
| 募集人数 | headcount | nullable / integer / min:0 | 正しい人数を入力してください |
| 参画開始時期 | start_date | nullable / date | 正しい日付を入力してください（YYYY-MM-DD） |
| 単価下限 | rate_min | nullable / integer / min:0 | 正しい金額を入力してください |
| 単価上限 | rate_max | nullable / integer / min:0 / gte:rate_min | 単価下限以上の値を入力してください |
| 単価備考 | rate_note | nullable / string / max:100 | - |
| 商流 | commercial_flow | nullable / in:prime,secondary,tertiary,other | 不正な商流値です（prime/secondary/tertiary/other） |
| 稼働形態 | work_style | nullable / in:onsite,hybrid,remote | 不正な稼働形態値です（onsite/hybrid/remote） |
| 勤務地（路線） | work_location_line | nullable / string / max:100 | - |
| 勤務地（最寄駅） | work_location_station | nullable / string / max:100 | - |
| 面談回数 | interview_count | nullable / integer / min:0 | 正しい回数を入力してください |
| 顧客折衝経験要否 | negotiation_required | nullable / in:0,1 | 0または1を入力してください |
| 業務内容詳細 | description | nullable / string | - |
| 稼働環境 | work_env | nullable / string | - |
| 精算幅 | billing_range | nullable / string / max:100 | - |
| 特記事項 | remarks | nullable / string | - |
| ステータス | status | required / in:open,closed,pending | ステータスは必須です（open/closed/pending） |
| 主担当ID | main_user_id | required / integer / exists:users,id | 主担当に存在するユーザーIDを指定してください |
| サブ担当ID | sub_user_id | nullable / integer / exists:users,id / different:main_user_id | 主担当と異なるユーザーIDを指定してください |
| 要件定義対象 | proc_requirements | nullable / in:0,1 | 0または1を入力してください |
| 基本設計対象 | proc_basic_design | nullable / in:0,1 | 0または1を入力してください |
| 詳細設計対象 | proc_detail_design | nullable / in:0,1 | 0または1を入力してください |
| 開発対象 | proc_development | nullable / in:0,1 | 0または1を入力してください |
| テスト対象 | proc_testing | nullable / in:0,1 | 0または1を入力してください |
| 保守運用対象 | proc_maintenance | nullable / in:0,1 | 0または1を入力してください |

> **スキルについて**：現時点ではCSV対象外とする。スキルの登録・編集は案件詳細画面から行うこと。スキルキーワードはエクスポートの絞り込み条件としては使用可能（TBD#1参照）。

**更新行（id列に既存ID）のバリデーション**

| 項目 | 内容 |
|---|---|
| id | integer / exists:projects,id。存在しないIDは全行ロールバックしてエラーを返す |
| 他フィールド | 新規追加行と同じバリデーションルールを適用 |

---

### GET /csv/engineers/export・GET /csv/projects/export クエリパラメータのバリデーション

**人材エクスポート**

| パラメータ | バリデーション | エラーメッセージ例 |
|-----------|-------------|----------------|
| status | nullable / array | - |
| status.* | in:proposable,interviewing,not_proposable | 不正なステータス値です |
| user_id | nullable / integer / exists:users,id | 存在するユーザーIDを指定してください |
| available_from_start | nullable / date | 正しい日付を入力してください |
| available_from_end | nullable / date / after_or_equal:available_from_start | 開始日以降の日付を入力してください |
| keyword | nullable / string / max:100 | - |
| work_styles | nullable / array | - |
| work_styles.* | in:onsite,hybrid,remote | 不正な勤務形態値です |

**案件エクスポート**

| パラメータ | バリデーション | エラーメッセージ例 |
|-----------|-------------|----------------|
| status | nullable / array | - |
| status.* | in:open,closed,pending | 不正なステータス値です |
| user_id | nullable / integer / exists:users,id | 存在するユーザーIDを指定してください |
| start_date_from | nullable / date | 正しい日付を入力してください |
| start_date_to | nullable / date / after_or_equal:start_date_from | 開始日以降の日付を入力してください |
| keyword | nullable / string / max:100 | - |
| work_style | nullable / array | - |
| work_style.* | in:onsite,hybrid,remote | 不正な稼働形態値です |

---

## 9. マスタ管理（Master）

---

## 未確定事項（TBD）

| # | 項目 | QA# | 理由 |
|---|------|-----|------|
| 1 | アピールポイントの文字数制限 | - | 職務経歴書の削除に伴い拡張（現時点：4000文字・暫定） |
