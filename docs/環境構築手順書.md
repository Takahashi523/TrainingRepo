# 環境構築手順書

> 対象者：開発メンバー全員  
> 前提：リポジトリの初期セットアップが完了済みであること

---

## 凡例

手順書内の各セクションには以下のラベルを付与しています。

| ラベル | 意味 |
|---|---|
| 🖥️ ホスト | ホスト OS のターミナルで実行する操作 |
| 🐳 Dev Container | VS Code の Dev Container 内ターミナルで実行する操作 |
| 🖱️ VS Code 操作 | VS Code の GUI 操作 |

---

## 前提条件

以下の2つのみホスト OS にインストールしてください。  
それ以外（Node.js・PHP・Python 等）は**一切インストール不要**です。

| ツール | 用途 | インストール先 |
|---|---|---|
| Docker Desktop | コンテナ実行基盤 | https://www.docker.com/products/docker-desktop/ |
| VS Code | エディタ | https://code.visualstudio.com/ |

### VS Code 拡張機能のインストール

- **Dev Containers**（`ms-vscode-remote.remote-containers`）

---

## 1. リポジトリのクローン　🖥️ ホスト

```bash
git clone git@github.com:<organization>/TrainingRepo.git
```

---

## 2. VS Code でフォルダを開く　🖱️ VS Code 操作

> **注意：** ファイルを直接開くのではなく、**フォルダ単位**で開いてください。

1. VS Code のメニューから `ファイル` → `フォルダを開く`
2. クローンした `TrainingRepo/` フォルダを選択

---

## 3. 依存パッケージのインストール（全員）　🖥️ ホスト

初回クローン直後は `vendor/` が存在しないため、Dev Container を起動する前に依存パッケージをインストールします。

```bash
cd laravel
docker-compose run --rm laravel sh
composer install
npm install
exit
docker-compose down
```

---

## 4. Dev Container の起動　🖱️ VS Code 操作

1. VS Code の右下にポップアップが表示されます

```
「コンテナーで再度開く」
```

2. `コンテナーで再度開く` をクリック
3. 以下の選択ダイアログが表示されます

```
・SES Matching - Laravel  ← Laravel チームはこちらを選択
・SES Matching - Python   ← Python チームはこちらを選択
```

> **初回は10〜20分程度かかります。** 2回目以降はキャッシュが効くため1〜2分で起動します。

> **補足：** Dev Container の起動時にルート直下の `docker-compose.yml` に定義されたサービスが自動で立ち上がります。 （laravel・python・mysql・minio）

---

## 5. コンテナの起動（Dev Container を使わない場合のみ）　🖥️ ホスト

> **Dev Container を使う場合、§4 の起動時に全サービスが自動で立ち上がるため、この手順は不要です。**  
> Dev Container を使わずに作業する場合のみ、以下を実行してください。

### Laravel チーム

```bash
# リポジトリルートで実行
docker-compose up -d laravel mysql
```

| 起動サービス | 用途 |
|---|---|
| laravel | Laravel + React（Inertia.js）|
| mysql | データベース |

### Python チーム

```bash
# リポジトリルートで実行
docker-compose up -d python mysql
```

| 起動サービス | 用途 |
|---|---|
| python | FastAPI マッチングエンジン |
| mysql | マッチングスコア・データ参照用 DB |

### 全サービス統合起動（結合確認・統合テスト時）

```bash
# リポジトリルートで実行
docker-compose up -d
```

---

## 6. 環境変数の設定　🐳 Dev Container

```bash
cp -a ./.env.example ./.env

```

---

## 7. アプリケーションキーの生成（Laravel チームのみ）　🐳 Dev Container

```bash
php artisan key:generate
```

---

## 8. マイグレーションと初期データ投入（Laravel チームのみ）　🐳 Dev Container

> **⚠️ 注意：`php artisan migrate` が失敗する場合は `.env` を確認してください。**
> 初期状態の `.env` が SQLite のままになっていると MySQL コンテナに接続できません。
> `.env` の DB 設定が以下になっているか確認・修正してください。
>
> ```
> DB_CONNECTION=mysql
> DB_HOST=mysql
> DB_PORT=3306
> DB_DATABASE=ses_matching
> DB_USERNAME=root
> DB_PASSWORD=secret
> ```

```bash
php artisan migrate
php artisan db:seed
```

---

## 9. Vite 開発サーバーの起動（Laravel チームのみ）　🐳 Dev Container

別ターミナルを開いて実行してください。

```bash
npm run dev
```

---

## 10. テスト環境の設定（Laravel チームのみ）　🐳 Dev Container

`.env.testing` はコミット対象外のため、各自で作成が必要です。

```bash
cp .env .env.testing
```

コピー後、`.env.testing` を開いて以下の項目を書き換えてください。

```
APP_ENV=testing

DB_CONNECTION=sqlite
DB_DATABASE=:memory:

SESSION_DRIVER=array
CACHE_STORE=array
QUEUE_CONNECTION=sync
MAIL_MAILER=array
```

> **ℹ️ `ALLOWED_EMAIL_DOMAINS` について：** `cp .env .env.testing` でコピーすると `.env` の `ALLOWED_EMAIL_DOMAINS` も引き継がれますが、テスト実行時は `phpunit.xml` の `<env name="ALLOWED_EMAIL_DOMAINS" value=""/>` が先に空をセットし、後続の `.env.testing`（dotenv）は既存値を上書きしないため、テストでは常に空（＝ドメイン制限なし）に固定されます。`.env.testing` 側で設定・削除する必要はありません。
> これは、Factory が生成する `@example.*` メールが許可ドメインの `ends_with` 検証で弾かれ、各自のローカル値によってテストが不安定（flaky）になるのを防ぐためです。ドメイン制限そのものを検証するテストは、テスト内で `config(['organization.allowed_email_domains' => [...]])` を明示設定しているため影響を受けません。

設定後、テストが通ることを確認してください。

```bash
php artisan test
```

---

## 11. 動作確認

| サービス | URL | 確認内容 |
|---|---|---|
| Laravel | http://localhost:8000 | ログイン画面が表示される |
| Python | http://localhost:8001/docs | Swagger UI が表示される |

### ⚠️ トラブル：http://localhost:8000 にアクセスすると画面が真っ白になる場合

**原因：** Vite の HMR 接続先が `0.0.0.0` になっており、ブラウザから JS が読み込めていません。

`laravel/vite.config.ts` を以下のように修正してください。

```ts
export default defineConfig({
  server: {
    host: '0.0.0.0',
    port: 5173,
    hmr: {
      host: 'localhost',  // ← これを追加
    },
  },
})
```

修正後、`npm run dev` を再起動してください。

---

## 12. 初期ログイン情報（Laravel チームのみ）

### 開発・テスト環境

`php artisan db:seed` により、開発・テスト用の管理者アカウントが投入されます（`local` / `testing` 環境限定）。

| 項目 | 値 |
|---|---|
| メールアドレス | `test@example.com` |
| パスワード | `password` |

> **⚠️ この既知アカウントは `local` / `testing` 環境でのみ作成されます。** 本番（`production`）では `db:seed` を実行しても作成されません（`DatabaseSeeder` で環境ガード済み）。

### 本番環境の初期管理者（リリース時）

マスタ管理（`/master`）は管理者専用で自己登録もないため、リリース時は **Artisan コマンドで最初の管理者を作成**します。平文パスワードを `.env` やリポジトリに残さないよう、対話入力（`--password` 省略で非表示入力）で作成してください。

> **⚠️ リリース前チェック（必須）：`ALLOWED_EMAIL_DOMAINS` を必ず設定すること。**
> ユーザーのメールアドレス（＝ログインID）に許可する社内ドメインを `.env` にカンマ区切りで設定します（例：`ALLOWED_EMAIL_DOMAINS=example.co.jp,example.com`）。
> **未設定のまま本番稼働すると、ドメイン制限が完全に無効化され（形式チェックのみ）、任意ドメインのメールでユーザー登録できてしまいます。** `.env` は git 管理外のため設定漏れに気づきにくく、リリース担当者は初期管理者作成の前にこの設定を必ず確認してください。

```bash
# 対話形式（推奨・パスワードは非表示入力）
php artisan app:create-admin

# 引数指定（CI 等で自動化する場合）
php artisan app:create-admin --name="山田 太郎" --email="admin@example.com" --password="＜8文字以上・英字と数字＞"
```

- パスワードは `Password::defaults()`（8文字以上・英字と数字を含む）でバリデーションされます。
- メールアドレスは一意制約＋（`ALLOWED_EMAIL_DOMAINS` 設定時は）ドメイン制限が適用されます。
- 作成後は、この管理者でログインしてマスタ管理から他ユーザーを追加できます。

---

## 13. ライブラリの追加手順

### Laravel チーム：PHP ライブラリの追加（Composer）　🐳 Dev Container

PHP ライブラリは `composer.json` で管理されています。

```bash
# コンテナ内で実行
composer require <パッケージ名>

# 例：CSV 出力ライブラリの追加
composer require league/csv:^9.0
```

追加後は `composer.json` と `composer.lock` が更新されます。必ず Git にコミットしてください。

```bash
git add composer.json composer.lock
git commit -m "feat: league/csv を追加"
```

### Laravel チーム：JavaScript ライブラリの追加（npm）　🐳 Dev Container

JavaScript ライブラリは `package.json` で管理されています。

```bash
# コンテナ内で実行

# 本番用ライブラリ
npm install <パッケージ名>@<バージョン> --legacy-peer-deps

# 開発用ライブラリ
npm install <パッケージ名>@<バージョン> --save-dev --legacy-peer-deps

# 例：日付ライブラリの追加
npm install dayjs@1.11.10 --legacy-peer-deps
```

> **注意：** バージョンは必ず固定してください。https://www.npmjs.com でバージョンを確認してから追加します。

追加後は `package.json` と `package-lock.json` が更新されます。必ず Git にコミットしてください。

```bash
git add package.json package-lock.json
git commit -m "feat: dayjs を追加"
```

### Python チーム：Python ライブラリの追加（pip）　🐳 Dev Container

Python ライブラリは `requirements.txt` で管理されています（Node.js の `package.json` 相当）。

**手順1：`requirements.txt` にバージョンを固定して追記**

```
# python/requirements.txt
fastapi==0.115.0
uvicorn==0.30.0
...
numpy==2.0.0    # ← 追加したいライブラリをバージョン固定で追記
```

> **バージョンの確認方法：** https://pypi.org で検索して最新の安定版を確認します。

**手順2：コンテナを再起動（自動でインストールされる）**

```bash
docker-compose restart python
```

コンテナ起動時に `pip install -r requirements.txt` が自動実行されるため、追記したライブラリが自動でインストールされます。

**手順3：Git にコミット**

```bash
git add python/requirements.txt
git commit -m "feat: numpy を追加"
```

> **注意：** バージョンを固定せず `numpy` のように書くとメンバーごとに異なるバージョンが入る可能性があります。必ず `numpy==2.0.0` のようにバージョンを固定してください。

---

## 14. よくあるトラブルと対処法

### Dev Container が起動しない

フォルダ単位で開いているか確認してください。

### コンテナのログ確認　🖥️ ホスト

```bash
docker-compose logs laravel
docker-compose logs python
```

### マイグレーションが失敗する　🐳 Dev Container

```bash
php artisan migrate
```

### Python ライブラリが反映されない　🖥️ ホスト

`requirements.txt` に追記後にコンテナを再起動してください。

```bash
docker-compose restart python
docker-compose logs python   # インストールログを確認
```

---

## 15. よく使うコマンド

### Laravel チーム　🐳 Dev Container

```bash
# コンテナ起動した状態で、ホストからlaravelコンテナに入る（🖥️ ホスト）
docker exec -it trainingrepo-laravel-1 sh

# マイグレーション
php artisan migrate

# キャッシュクリア
php artisan config:clear
php artisan cache:clear

# ルート一覧確認
php artisan route:list

# テスト実行
php artisan test
```

### Python チーム　🐳 Dev Container

```bash
# テスト実行
cd /app && pytest

# インストール済みライブラリ一覧確認
pip list

# 特定ライブラリのバージョン確認
pip show sqlalchemy
```

---

## 16. コンテナの停止　🖥️ ホスト

```bash
# 停止（データは保持）
docker-compose stop

# 停止 + コンテナ削除（データは保持）
docker-compose down

# 停止 + データも全削除（DB・MinIO のデータが消えます）
docker-compose down -v
```

> **注意：** `-v` オプションはローカルの DB・MinIO データが全て消えます。必要な場合のみ使用してください。

---

## 17. やり直す場合　🖥️ ホスト

```bash
# コンテナ・ボリュームを全削除
docker-compose down -v

# Git 管理外ファイルを全削除（vendor・node_modules 等）
git clean -fdx

# 手順3からやり直す
```
